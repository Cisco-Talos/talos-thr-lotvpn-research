[tool.poetry]
name = "lott"
version = "0.1.0"
description = "Living Off the Tunnel (LOTT) - a reference for VPN applications used to evade detection and bypass network controls"

[tool.poetry.dependencies]
python = "^3.13"
pyyaml = "^6.0.3"
yamllint = "^1.38.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

